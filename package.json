{
  "name": "parazit",
  "version": "1.0.0",
  "description": "Паразит — стелс-платформер.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && http-server dist -c-1 -o",
    "start": "npm run dev",
    "serve": "http-server dist -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
